
## creating new project

instead of `pip install -r requirements.txt`, do `make install`

## posting new version

3. make changes
2. manually update CHANGELOG.md to just the most recent changes
3. manually update version.py
4. do a commit + push
5. 